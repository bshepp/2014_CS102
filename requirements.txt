# N-Dimensional Geometry Engine - Dependencies
# Updated: 2025-01-18
# Python 3.9+ compatible for CI/CD

# Core scientific computing (Python 3.9+ compatible)
numpy>=1.20.0,<2.0.0

# Utility libraries
packaging>=20.0

# Current runtime dependencies (verified compatible)
# All packages tested and working as of 2025-01-18

# Optional dependencies for enhanced functionality
# (required for web interface)
scipy>=1.7.0,<2.0.0
matplotlib>=3.3.0,<4.0.0
plotly>=5.0.0,<6.0.0
pandas>=1.3.0,<3.0.0

# Development dependencies
# (commented out - will be added for CI/CD)
# pytest>=7.0.0,<9.0.0
# black>=22.0.0,<25.0.0
# flake8>=4.0.0,<8.0.0
# mypy>=0.910,<2.0.0

# Web framework dependencies (for web deployment)
fastapi>=0.68.0,<1.0.0
uvicorn>=0.15.0,<1.0.0
pydantic>=1.8.0,<3.0.0
python-multipart>=0.0.5  # Required for FastAPI file uploads

# Note: Java dependencies managed separately via SDKMAN
# - OpenJDK 11.0.21 (LTS)
# - Compatible with original CS102 code