# N-Dimensional Geometry Engine - Dependencies
# Updated: 2025-01-18
# Python 3.9+ compatible for CI/CD - using 2024-2025 versions

# Core scientific computing (latest compatible with Python 3.9+)
numpy>=1.24.0,<2.0.0  # Latest 1.x series (NumPy 2.x requires Python 3.10+)

# Utility libraries
packaging>=24.0

# Current runtime dependencies (2024-2025 versions)
# All packages tested and working as of 2025-01-18

# Optional dependencies for enhanced functionality
# (required for web interface)
scipy>=1.11.0,<2.0.0
matplotlib>=3.8.0,<4.0.0
plotly>=5.18.0,<6.0.0
pandas>=2.1.0,<3.0.0

# Development dependencies
# (commented out - will be added for CI/CD)
# pytest>=8.0.0,<9.0.0
# black>=24.0.0,<25.0.0
# flake8>=7.0.0,<8.0.0
# mypy>=1.8.0,<2.0.0

# Web framework dependencies (for web deployment - 2024 versions)
fastapi>=0.108.0,<1.0.0
uvicorn>=0.25.0,<1.0.0
pydantic>=2.5.0,<3.0.0  # Pydantic 2.x works with Python 3.9+
python-multipart>=0.0.6  # Required for FastAPI file uploads

# Note: Java dependencies managed separately via SDKMAN
# - OpenJDK 11.0.21 (LTS)
# - Compatible with original CS102 code