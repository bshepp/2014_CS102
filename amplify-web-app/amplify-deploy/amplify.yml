version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting build for N-Dimensional Geometry Engine"
    build:
      commands:
        - echo "Building static site"
        - ls -la
    postBuild:
      commands:
        - echo "Build completed successfully"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []